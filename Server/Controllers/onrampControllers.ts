import { Request, Response } from "express";

// this function will check to ensure the mpesa message has been sent and trigger the agent to send the cUSD to the wallet
export const onrampToWallet= async (req:Request, res:Response)=>{
    try {
        
    } catch (error) {
        
    }

}